# airflow/requirements.txt
# Pinned Python dependencies for the Airflow workers and scheduler.
# Built into the Docker image at build time (not at container startup).
# Version-pinned for reproducibility.

apache-airflow==2.10.5

# HTTP client for ML Engine API calls
httpx==0.27.2

# Great Expectations data quality
great-expectations==0.18.14

# PostgreSQL adapter for Airflow backend
psycopg2-binary==2.9.9

# Redis client for distributed locking
redis==5.2.0

# Timezone-aware datetime
pendulum==3.0.0

# Data processing
pandas==2.2.2
numpy==1.26.4

# MLflow for model registry integration
mlflow==2.21.3

# DVC for data versioning
dvc==3.51.0

# MinIO / S3 client for artifact storage
boto3==1.35.36
