{{- if .Values.grafana.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-ml-inference-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/managed-by: {{ .Release.Service }}
data:
  ml-inference-dashboard.json: |
{{ .Files.Get "files/dashboards/ml-inference-dashboard.json" | indent 4 }}
{{- end }}
