# `is-a.dev` Resubmission Plan

## Status

This plan is obsolete for the active production contract.

- Canonical public frontend: `https://tradergunit.pages.dev/`
- Current Contabo runtime proof hosts:
  - `https://173.249.18.14.sslip.io`
  - `https://bff.173.249.18.14.sslip.io/health`
  - `https://api.173.249.18.14.sslip.io/health`
- `is-a.dev` is not the active path and must not be treated as a Stage P
  blocker unless a future hostname migration is explicitly reopened.

## Why This File Still Exists

The old `is-a.dev` work is kept only as historical context so future searches
do not make it look like a missing deliverable.

If a branded hostname is ever revived later, write a new migration runbook from
the current `tradergunit.pages.dev` + Contabo runtime contract instead of
resurrecting the retired plan from this file.
